server {
        listen 80;
        server_name {{ domain_name }};

        root /var/www/{{ domain_name }}/html;
        index index.html;


        location / {
                try_files $uri $uri/ =404;
        }
}
